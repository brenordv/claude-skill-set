# Markdown lint

After editing any `.md` file in this repo, run `tools/lint-repo.ps1` (Windows) or
`tools/lint-repo.sh` and fix the findings before handing off. CI runs the same script on every
push and pull request; `tools/README.md` explains what it checks and why.
